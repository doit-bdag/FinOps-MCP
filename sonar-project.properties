sonar.projectKey=doit-bdag_FinOps-MCP
sonar.organization=doit-bdag

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=FinOps-MCP
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src
sonar.tests=tests

# Coverage and test results (these would require coverage xml files if we use pytest-cov, but keeping it simple for now)
# sonar.python.coverage.reportPaths=coverage.xml

# Language
sonar.language=py
